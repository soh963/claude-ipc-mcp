# IPC Command for Gemini CLI

description = "🛑 Stop global IPC broker"
prompt = """
Stop global IPC broker:

!{uv run python D:/claude-ipc-mcp/tools/ipc_global_command.py broker stop}

This gracefully shuts down the broker while preserving messages.
"""
