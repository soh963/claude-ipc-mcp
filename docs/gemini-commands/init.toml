# IPC Command for Gemini CLI

description = "🚀 Initialize IPC project structure"
prompt = """
Initialize IPC project structure:

!{uv run python D:/claude-ipc-mcp/tools/ipc_global_command.py init}

This creates:
- .ipc/ directory structure
- config, logs, state, secret subdirectories
- Project-specific IPC configuration
"""
