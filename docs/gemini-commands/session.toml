# IPC Command for Gemini CLI

description = "📋 Show current session info"
prompt = """
Show current session info:

!{uv run python D:/claude-ipc-mcp/tools/ipc_global_command.py session}

Shows:
- Instance ID
- Session token status
- Creation and activity times
- Project directory
"""
