# IPC Command for Gemini CLI

description = "🔄 Reset all instances"
prompt = """
Reset all instances:

!{uv run python D:/claude-ipc-mcp/tools/ipc_global_command.py instances reset}

⚠️ WARNING: This will delete ALL instance registrations!

This removes all instances from the broker. Messages are preserved.
"""
