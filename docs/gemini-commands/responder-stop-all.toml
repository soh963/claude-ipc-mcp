# IPC Command for Gemini CLI

description = "🛑 Stop all auto-responders"
prompt = """
Stop all auto-responders:

!{uv run python D:/claude-ipc-mcp/tools/ipc_global_command.py responder stop-all}

This terminates all active responder processes gracefully.
"""
