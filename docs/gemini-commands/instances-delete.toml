# IPC Command for Gemini CLI

description = "🗑️ Delete specific instance"
prompt = """
Delete a specific instance registration.

Instance name to delete: {args[0]}

!{uv run python D:/claude-ipc-mcp/tools/ipc_global_command.py instances delete {args[0]}}

This removes the instance from the broker's registry.
"""
