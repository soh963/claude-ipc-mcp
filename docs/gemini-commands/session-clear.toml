# IPC Command for Gemini CLI

description = "🧹 Clear current session"
prompt = """
Clear current session:

!{uv run python D:/claude-ipc-mcp/tools/ipc_global_command.py session clear}

This removes session token and requires re-registration.
"""
