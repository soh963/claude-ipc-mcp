# IPC Command for Gemini CLI

description = "🚀 Start all auto-responders"
prompt = """
Start auto-responders for all registered instances:

!{uv run python %IPC_CHAT%\\tools\\ipc_global_command.py responder start-all --policy smart --detach}

This starts background responders for every registered instance.
"""
