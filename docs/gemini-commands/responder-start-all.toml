# IPC Command for Gemini CLI

description = "🚀 Start all auto-responders"
prompt = """
Start all auto-responders:

!{uv run python D:/claude-ipc-mcp/tools/ipc_global_command.py responder start-all --policy smart --detach}

This starts background responders for every registered instance.
"""
