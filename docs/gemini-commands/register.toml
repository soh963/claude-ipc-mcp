# IPC Command for Gemini CLI

description = "📝 Register this instance with IPC"
prompt = """
Register this instance with the IPC broker.

What instance name would you like to use? (e.g., gemini-main, gemini-assistant)

[After receiving the name, execute:]
!{uv run python %IPC_CHAT%\\tools\\ipc_global_command.py register {instance_name}}

This:
- Connects to the broker
- Receives a session token
- Enables message sending/receiving
"""
