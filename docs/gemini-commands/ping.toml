# IPC Command for Gemini CLI

description = "🔔 Test broker connectivity"
prompt = """
Test broker connectivity:

!{uv run python D:/claude-ipc-mcp/tools/ipc_global_command.py ping}

This verifies:
- Broker is running
- Network connectivity
- Authentication status
"""
