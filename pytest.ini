[pytest]
minversion = 8.0
addopts = -q
# Only run contract and unit tests by default; integration/e2e are opted-in
testpaths =
	tests/contract
	tests/unit
norecursedirs = backup test legacy_unmaintained redundant_* tests/integration tests/e2e